package cn.unclezhang.conf;

import java.util.List;

public class Optimization {
/*	
	public void setNulls(JsonOptimizable jopt, String fields[]) {
		for (int i = 0; i < fields.length; i++) {
			if(jopt.getOptimizableFields().contains(fields[i])) {
				jopt.getOptimizableFields().get(i);
			}
		}
	}
	
	public interface JsonOptimizable {
		List<Object> getOptimizableFields();
	}
	
*/}
